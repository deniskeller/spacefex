import React from 'react';
import { Default } from '@layouts/index';
import { SystemStatus } from '@view/index';

const SystemStatusPage = () => {
  return (
    <Default>
      <SystemStatus />
    </Default>
  );
};

export default SystemStatusPage;
