import React from 'react';
import { Default } from '@layouts/index';
import { Developers } from '@view/index';

const DevelopersPage = () => {
  return (
    <Default>
      <Developers />
    </Default>
  );
};

export default DevelopersPage;
