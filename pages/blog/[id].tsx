import React from 'react';
import { Default } from '@layouts/index';
import { BlogSpecific } from '@view/index';

const BlogSpecificPage = () => {
  return (
    <Default>
      <BlogSpecific />
    </Default>
  );
};

export default BlogSpecificPage;
