import React from 'react';
import { Default } from '@layouts/index';
import { Foundation } from '@view/index';

const FoundationPage = () => {
  return (
    <Default>
      <Foundation />
    </Default>
  );
};

export default FoundationPage;
